import React, { FunctionComponent } from "react";
import { View } from "react-native";

const SummaryScreen: FunctionComponent = () => {
    return (
        <View style={{ flex: 1, backgroundColor: 'blue' }}>
        </View>
    );
};

export default SummaryScreen;
